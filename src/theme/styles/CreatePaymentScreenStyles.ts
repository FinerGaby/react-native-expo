import { StyleSheet } from "react-native";
import { primary } from "../colors";

export const CreatePaymentScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: primary.white,
    padding: 24,
  },
  button: {
    marginTop: "auto",
  },
});
